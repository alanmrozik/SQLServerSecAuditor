<Window x:Class="SqlSecAuditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SQLSecAuditor" Height="800" Width="1000"
        Background="#F4F4F4"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="ReportBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DCDCDC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style x:Key="SidebarTabItemStyle" TargetType="TabItem">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="15,0,15,10"/>
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" 
                                Background="#F8F8F8" 
                                BorderBrush="#C0C0C0" 
                                BorderThickness="1" 
                                CornerRadius="2">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"
                                              Margin="10,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E5E5E5" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#A0A0A0" />
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#EEEEEE" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NumberButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="0,0,5,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DCDCDC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5,2">
            <MenuItem Header="_Plik">
                <MenuItem Header="_Zakończ" />
            </MenuItem>
            <MenuItem Header="_Edycja"/>
            <MenuItem Header="_Widok"/>
            <MenuItem Header="P_omoc"/>
        </Menu>

        <TabControl Grid.Row="1" 
            TabStripPlacement="Left" 
            Background="Transparent" 
            BorderThickness="0" 
            Padding="0"
            ItemsSource="{Binding Instances}">
            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="White" BorderBrush="#DCDCDC" BorderThickness="1" CornerRadius="3" Margin="10">
                            <StackPanel IsItemsHost="True" Margin="0,20,0,0"/>
                        </Border>
                        <ContentPresenter Grid.Column="1" ContentSource="SelectedContent" />
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>

            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem" BasedOn="{StaticResource SidebarTabItemStyle}">
                    <Setter Property="Header" Value="{Binding ServerName}"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource ReportBorderStyle}">
                        <Grid Margin="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                <Label Content="Raport dla: " FontSize="18" FontFamily="Arial" FontWeight="Bold"/>
                                <Label Content="{Binding ServerName}" FontSize="18" FontFamily="Arial" FontWeight="Bold" Foreground="DarkBlue"/>
                            </StackPanel>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <Expander IsExpanded="True" BorderBrush="#DCDCDC" BorderThickness="1" Margin="0,0,0,10">
                                        <Expander.Header>
                                            <Label Content="General" FontSize="16" Padding="10,5,0,5"/>
                                        </Expander.Header>
                                        <TextBlock Text="{Binding GeneralInfo}" Margin="10" TextWrapping="Wrap"/>
                                    </Expander>

                                    <Expander IsExpanded="True" BorderBrush="#DCDCDC" BorderThickness="1" Margin="0,0,0,10">
                                        <Expander.Header>
                                            <Label Content="Permissions" FontSize="16" Padding="10,5,0,5"/>
                                        </Expander.Header>
                                        <StackPanel Margin="10">
                                            <TextBlock Text="Wykryte błędy uprawnień:" Margin="0,0,0,5" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding PermissionsInfo}" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                            <WrapPanel Orientation="Horizontal">
                                                <Button Content="1" Style="{StaticResource NumberButtonStyle}"/>
                                                <Button Content="2" Style="{StaticResource NumberButtonStyle}"/>
                                                <Button Content="3" Style="{StaticResource NumberButtonStyle}"/>
                                            </WrapPanel>
                                        </StackPanel>
                                    </Expander>
                                </StackPanel>
                            </ScrollViewer>

                            <Button Grid.Row="2" Content="EXPORT" Width="100" Height="30" HorizontalAlignment="Right" Background="White" BorderBrush="#C0C0C0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>